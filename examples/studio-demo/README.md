# Studio Demo

This fixture gives Studio a small React Native workspace to open without leaving
the monorepo. It includes Expo Router screens, `.rncanvas` sidecars, a flow
manifest, tokens, assets, and a local component import so repo scanning and sync
paths have realistic data to exercise.

Use Studio's **Open demo** command in the Repository panel to connect to this
fixture quickly.
